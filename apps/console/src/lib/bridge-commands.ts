// Shared bridge-daemon command strings. Keeping them in one file avoids
// duplicating the same literals in RuntimesList and ConnectMachineInstructions.
export const SETUP_CMD = "npx @getoma/cli bridge setup";
export const RECONNECT_CMD = "npx @getoma/cli bridge restart";
