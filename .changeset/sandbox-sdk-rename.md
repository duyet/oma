---
"@getoma/cli": patch
---

Internal import rename: `@duyet/oma-sandbox` → `@getoma/sandbox-sdk` in the bridge daemon's OpenShell client. No published-behavior change — the adapter resolves from the workspace at runtime, not from npm.
