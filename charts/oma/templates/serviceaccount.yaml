{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "oma.serviceAccountName" . }}
  labels:
    {{- include "oma.labels" . | nindent 4 }}
{{- end }}
